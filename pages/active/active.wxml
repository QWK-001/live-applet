<import src="../../template/card/card.wxml" />
<view class="page">
  <view class="section" wx:for="{{liveroomsList}}" wx:key="{{item.labelId}}">
    <template is="activeCard" data="{{data: item.data}}"></template>
  </view>
</view>